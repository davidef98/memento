name: books_api
description: The interface and models for an API providing access to books and related objects.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  equatable: ^2.0.8
  json_annotation: ^4.9.0
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.11.2
  mocktail: ^1.0.4
